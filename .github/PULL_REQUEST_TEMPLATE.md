## What and why

<!-- What changes, and the reason. The diff already says what; this should
     say why it is worth doing. -->

## How it was checked

- [ ] `make check` passes
- [ ] a bug fix has a regression test that fails without the fix
- [ ] a user-visible change was tried in a browser, normal path and at least
      two edge cases
- [ ] a rule a machine could enforce is enforced by a machine (lint or test)

## Anything a reviewer should look at closely

<!-- Something you are unsure about, a tradeoff you made, a case you chose
     not to handle. Say it here rather than waiting to be asked. -->
